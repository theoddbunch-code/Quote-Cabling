{
  "name": "Quote-Cabling",
  "version": "1.0.0",
  "description": "A project for generating sitewalk forms and sending them via email using Netlify and serverless functions.",
  "main": "app.js",
  "scripts": {
    "start": "netlify dev",       // For local development
    "build": "echo 'No build required'", // No build command if purely static frontend
    "deploy": "netlify deploy"   // To manually deploy to Netlify
  },
  "dependencies": {
    "nodemailer": "^6.9.1",      // For sending emails
    "pdfkit": "^0.13.0"          // For generating PDFs
  },
  "devDependencies": {
    "netlify-cli": "^15.0.0"     // For local testing of Netlify functions
  },
  "keywords": ["netlify", "serverless", "pdf", "email"],
  "author": "Your Name",
  "license": "MIT"
}
